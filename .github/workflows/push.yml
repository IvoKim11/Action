name: Push

on: 
  push:
    branches:
      - dev
  workflow_dispatch: 

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: |
          echo "Hello World"
          ls -la